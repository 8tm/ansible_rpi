---
# Rola "apt": dba o to, żeby cache APT był świeży dla dalszych ról.
# Idempotentna: używa modułu apt z update_cache i cache_valid_time.

- name: Refresh APT cache (once per day)
  apt:
    update_cache: yes
    cache_valid_time: 86400  # 24h
